# Optionaler Vergleichsarm: fertiges deutsches NER-Modell ohne Anpassung
# (scripts/07_flair_baseline.py).
#
# Bewusst nicht in requirements.txt: flair bringt gut zwei Dutzend Pakete mit
# (boto3, gdown, langdetect, lxml ...), die für Pipeline und Training keine
# Rolle spielen. Wer nur trainiert, soll sie nicht installieren müssen.
#
# Geprüft am 29.07.2026 gegen das Projekt-venv: flair 0.15.1 lässt numpy,
# torch, transformers und tokenizers unangetastet, die Installation ist rein
# additiv. Vor einem Versionssprung lohnt sich derselbe Test:
#   pip install --dry-run -r requirements-flair.txt
#
# Installation nur, wenn der Baseline-Arm gebraucht wird:
#   pip install -r requirements-flair.txt
flair>=0.13
