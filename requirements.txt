# Daten-Pipeline
requests
pymupdf
tqdm
python-dotenv
openai

# Modelltraining & Evaluation
torch
# transformers 5.x hat die Slow-Tokenizer entfernt; deepset/gbert-base liefert
# aber nur vocab.txt und lässt sich dort nicht mehr laden -> bei 4.x bleiben.
transformers>=4.44,<5
accelerate>=0.26          # vom HF-Trainer vorausgesetzt, kommt nicht automatisch mit
sentencepiece
pillow                    # Seitenbilder fuer den visuellen Backbone von LayoutXLM
numpy
seqeval

# Vergleichsarm mit fertigem deutschen NER-Modell (scripts/07_flair_baseline.py):
# separat installieren, siehe requirements-flair.txt

# LayoutXLM (LayoutLMv2-Architektur) braucht zusätzlich detectron2 für den
# visuellen Backbone. Installation ist plattformabhängig, siehe:
# https://detectron2.readthedocs.io/en/latest/tutorials/install.html
# detectron2

# Frontend-API
fastapi
uvicorn
python-multipart

# Entwicklung
pytest
httpx
